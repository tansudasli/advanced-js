// in a object format, it is easy to understand what is what
const drink = {
    color: 'brown',
    carbonated: true,
    sugar: 40
};

// tuple = arrays w/ type
// suitable mostly for csv files
// harder to understand what is what..
const pepsi : [string, boolean, number] = ['brown', true, 50];
